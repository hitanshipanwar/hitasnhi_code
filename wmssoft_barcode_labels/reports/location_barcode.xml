<?xml version="1.0" encoding="utf-8" ?>
    <odoo>
        <template id="stock.report_generic_barcode">
            <t t-call="web.html_container">
                <t t-if="len(docs) % 2 == 0">
                    <t t-set='nRows' t-value='int(len(docs) / 2)'/>
                </t>
                <t t-else="">
                    <t t-set='nRows' t-value='int((len(docs) / 2) + 1)'/>
                </t>
                <t t-set='nCols' t-value='2'/> 
                <div t-foreach="[docs[x:x + nRows * nCols] for x in range(0, len(docs), nRows * nCols)]" t-as="page_docs" class="page article">
                <t t-if="title">
                <h2 style="text-align: center; font-size: 3em"><t t-esc="title"/></h2>
                </t>
                <table>
                    <t t-foreach="range(nRows)" t-as="row">
                        <tr style="width: 384px !important;height: 180px !important;max-width: 385px !important;max-height: 181px !important;">
                            <t t-foreach="range(nCols)" t-as="col">
                                <t t-set="barcode_index" t-value="(row * nCols + col)"/>
                                <t t-if="barcode_index &lt; len(page_docs)">
                                    <t t-set="o" t-value="page_docs[barcode_index]"/>
                                    <td>
                                        <div style="text-align: center; font-size: 4em"><span t-esc="o.name"/></div>
                                        <div t-if="o.barcode"><img t-att-src="'/report/barcode/?type=%s&amp;value=%s&amp;width=%s&amp;height=%s' % ('Code128', o.barcode, 600, 100)"/></div>
                                        <div style="text-align: center; font-size: 1em"><span t-esc="o.barcode"/></div>
                                    </td>
                                </t>
                            </t>
                        </tr>
                    </t>
                </table>
            </div>
            </t>
        </template>

    </odoo>
