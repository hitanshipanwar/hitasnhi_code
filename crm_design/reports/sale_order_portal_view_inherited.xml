<?xml version="1.0" encoding="UTF-8"?>
<odoo>
	<template id="sale_order_content_portal_inherited" inherit_id="sale.sale_order_portal_content">
	
		 <xpath  expr="//div[@t-if='sale_order.validity_date']" position="after">
            <div class="mb-3 col-6">
                <strong>Subject:</strong>
                <span t-field="sale_order.sizing"/>
            </div>
        </xpath>


		<!-- Replace Price -->
		<xpath	expr="//section[@id='details']" position="replace">

            <strong t-if="sale_order.extra_notes">Extra Notes:</strong>
            <p class="m-0" t-field="sale_order.extra_notes"/>
            
			<t t-if="sale_order.state == 'final_approved' or sale_order.state == 'sale' or not sale_order.need_design">
				<section id="details" style="page-break-inside: auto;" class="mt32">
                <h3 id="details">Pricing</h3>

                <t t-set="display_discount" t-value="True in [line.discount > 0 for line in sale_order.order_line]"/>

                <table t-att-data-order-id="sale_order.id" t-att-data-token="sale_order.access_token" class="table table-sm" id="sales_order_table">
                    <thead class="bg-100">
                        <tr>
                            <th class="text-left">Products</th>
                            <th class="text-right">Quantity</th>
                            <th t-attf-class="text-right {{ 'd-none d-sm-table-cell' if report_type == 'html' else '' }}">Unit Price</th>
                            <th t-if="display_discount" t-attf-class="text-right {{ 'd-none d-sm-table-cell' if report_type == 'html' else '' }}">
                                <span>Disc.%</span>
                            </th>
                            <th t-attf-class="text-right {{ 'd-none d-md-table-cell' if report_type == 'html' else '' }}">
                                <span>Taxes</span>
                            </th>
                            <th class="text-right" >
                                <span groups="account.group_show_line_subtotals_tax_excluded">Amount</span>
                                <span groups="account.group_show_line_subtotals_tax_included">Total Price</span>
                            </th>
                        </tr>
                    </thead>
                    <tbody class="sale_tbody">

                        <t t-set="current_subtotal" t-value="0"/>

                        <t t-foreach="sale_order.order_line" t-as="line">

                            <t t-set="current_subtotal" t-value="current_subtotal + line.price_subtotal" groups="account.group_show_line_subtotals_tax_excluded"/>
                            <t t-set="current_subtotal" t-value="current_subtotal + line.price_total" groups="account.group_show_line_subtotals_tax_included"/>

                            <tr t-att-class="'bg-200 font-weight-bold o_line_section' if line.display_type == 'line_section' else 'font-italic o_line_note' if line.display_type == 'line_note' else ''">
                                <t t-if="not line.display_type">
                                    <td id="product_name"><span t-field="line.name"/></td>
                                    <td class="text-right">
                                        <div id="quote_qty">
                                            <span t-field="line.product_uom_qty"/>
                                            <span t-esc="line.is_thai"/>
                                            <span t-esc="line.product_uom.thai_uom_name"/>
                                            <t t-if="line.is_thai and line.product_uom.thai_uom_name">
                                                <span t-field="line.product_uom.thai_uom_name"/>
                                            </t>
                                            <t t-else="">
                                                <span t-field="line.product_uom"/>
                                            </t>
                                        </div>
                                    </td>
                                    <td t-attf-class="text-right {{ 'd-none d-sm-table-cell' if report_type == 'html' else '' }}">
                                        <div
                                            t-if="line.discount &gt;= 0"
                                            t-field="line.price_unit"
                                            t-att-style="line.discount and 'text-decoration: line-through' or None"
                                            t-att-class="(line.discount and 'text-danger' or '') + ' text-right'"
                                        />
                                        <div t-if="line.discount">
                                            <t t-esc="(1-line.discount / 100.0) * line.price_unit" t-options='{"widget": "float", "decimal_precision": "Product Price"}'/>
                                        </div>
                                    </td>
                                    <td t-if="display_discount" t-attf-class="text-right {{ 'd-none d-sm-table-cell' if report_type == 'html' else '' }}">
                                        <strong t-if="line.discount &gt; 0" class="text-info">
                                            <t t-esc="((line.discount % 1) and '%s' or '%d') % line.discount"/>%
                                        </strong>
                                    </td>
                                    <td t-attf-class="text-right {{ 'd-none d-md-table-cell' if report_type == 'html' else '' }}">
                                        <span t-esc="', '.join(map(lambda x: (x.description or x.name), line.tax_id))"/>
                                    </td>
                                    <td class="text-right">
                                        <span class="oe_order_line_price_subtotal" t-field="line.price_subtotal" groups="account.group_show_line_subtotals_tax_excluded"/>
                                        <span class="oe_order_line_price_total" t-field="line.price_total" groups="account.group_show_line_subtotals_tax_included"/>
                                    </td>
                                </t>
                                <t t-if="line.display_type == 'line_section'">
                                    <td colspan="99">
                                        <span t-field="line.name"/>
                                    </td>
                                    <t t-set="current_section" t-value="line"/>
                                    <t t-set="current_subtotal" t-value="0"/>
                                </t>
                                <t t-if="line.display_type == 'line_note'">
                                    <td colspan="99">
                                        <span t-field="line.name"/>
                                    </td>
                                </t>
                            </tr>

                            <t t-if="current_section and (line_last or sale_order.order_line[line_index+1].display_type == 'line_section')">
                                <tr class="is-subtotal text-right">
                                    <td colspan="99">
                                        <strong class="mr16">Subtotal</strong>
                                        <span
                                            t-esc="current_subtotal"
                                            t-options='{"widget": "monetary", "display_currency": sale_order.pricelist_id.currency_id}'
                                        />
                                    </td>
                                </tr>
                            </t>
                        </t>
                    </tbody>
                </table>

                <div id="total" class="row" name="total" style="page-break-inside: avoid;">
                    <div t-attf-class="#{'col-4' if report_type != 'html' else 'col-sm-7 col-md-5'} ml-auto">
                        <t t-call="sale.sale_order_portal_content_totals_table"/>
                    </div>
                </div>
            </section>
			</t>
		</xpath>

		<xpath	expr="//section[@id='terms']" position="replace">
			<t t-if="sale_order.state == 'final_approved' or not sale_order.need_design">
				<section id="terms" class="mt-5" t-if="not is_html_empty(sale_order.note)">
					<h3 class="">Terms &amp; Conditions</h3>
		            <hr class="mt-0 mb-1"/>
		                <t t-if="sale_order.terms_type == 'html'">
		                    <!-- Note is plain text. This ensures a clickable link  -->
		                    <t t-set="tc_url" t-value="'%s/terms' % (sale_order.get_base_url())"/>
		                    <em>Terms &amp; Conditions: <a href="/terms"><t t-esc="tc_url"/></a></em>
		                </t>
		                <t t-else="">
		                    <em t-field="sale_order.note"/>
		                </t>
	            </section>
            </t>
		</xpath>

		<xpath	expr="//section[@t-if='sale_order.payment_term_id']" position="replace">
			<t t-if="sale_order.state == 'final_approved' or not sale_order.need_design">
				<section class="mt-5" t-if="sale_order.payment_term_id">
	                <h3 class="">Payment terms</h3>
	                <hr class="mt-0 mb-1"/>
	                <span t-field="sale_order.payment_term_id"/>
	            </section>
	        </t>
	    </xpath>

	</template>

	<!-- Hide Amount from portal slidebar -->
	<template id="sale_order_portal_template_inherited" inherit_id="sale.sale_order_portal_template">
		<xpath expr="//t[@t-set='title']" position="replace">
			<t t-if="state in ('final_approved','sale') or not sale_order.need_design">
				<t t-set="title">
                    <h2 class="mb-0"><b t-field="sale_order.amount_total" data-id="total_amount"/> </h2>
                </t>
                <!-- <t t-call="web.sign_name_and_signature"/> -->
                <!-- <t t-call="portal.signature_form"/> -->
			</t>
		</xpath>

		<xpath expr="//div[@id='modaldecline']/div/form/main/textarea" position="replace">
                <t t-call="portal.signature_form">
                                        <t t-set="call_url" t-value="sale_order.get_portal_url(suffix='/decline',query_string='&amp;type=reject')"/>
                                        <t t-set="send_label" t-value="Reject"/>
                                        <t t-set="default_name" t-value="sale_order.partner_id.name"/>
                                    </t>
			
		</xpath>
		<xpath expr="//div[@id='modaldecline']/div/form/footer" position="replace">
                <!-- <attribute name="invisible">1</attribute>                         -->
			
		</xpath>
	</template>

    <template id="sale_management.sale_order_portal_content_inherit_sale_management" name="Order Options" inherit_id="sale.sale_order_portal_content">

        <xpath expr="//section[@id='details']//div[@id='quote_qty']" position="replace">
            <t t-if="sale_order.state in ['draft', 'sent'] and line.sale_order_option_ids">
                <div class="input-group js_quantity_container pull-right">

                    <span class="d-print-none input-group-text d-none d-md-inline-block">
                        <a t-att-data-line-id="line.id" t-att-data-remove="True" href="#" class="js_update_line_json" aria-label="Remove one" title="Remove one">
                            <span class="fa fa-minus"/>
                        </a>
                    </span>
                    <!-- TODO add uom in this case too -->
                    <input type="text" class="js_quantity form-control" t-att-data-line-id="line.id" t-att-value="line.product_uom_qty"/>
                    <span class="d-print-none input-group-text d-none d-md-inline-block">
                        <a t-att-data-line-id="line.id" href="#" class="js_update_line_json" aria-label="Add one" title="Add one">
                            <span class="fa fa-plus"/>
                        </a>
                    </span>
                </div>
            </t>
            <t t-else="">
                <span t-field="line.product_uom_qty"/>
                <t t-if="line.is_thai and line.product_uom.thai_uom_name">
                    <span t-field="line.product_uom.thai_uom_name"/>
                </t>
                <t t-else="">
                    <span t-field="line.product_uom"/>
                </t>
            </t>
        </xpath>

    </template>
</odoo>